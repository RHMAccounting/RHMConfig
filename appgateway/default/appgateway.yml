zuul:
  prefix: /api
  sensitiveHeaders: Cookie,Set-Cookie

  routes:
    appuser:
      path: /user/**
      serviceId: appuser
      stripPrefix: true

feign:
  hystrix:
    enabled: true
hystrix:
  shareSecurityContext: true
  command:
    default:
      execition:
        timeout:
          enabled: false